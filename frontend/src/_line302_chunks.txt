CHUNK 0 len=200
      </div></section> : page === 'rooms' ? <section className="section"><div className="section-top"><div><p className="eyebrow">ROOMS AVAILABLE</p><h2>{selectedHotel?.name}</h2><p>{selectedHotel?.ci
CHUNK 1 len=200
ty}</p></div><div className="section-actions"><button className="ghost" onClick={() => setPage('home')}>Back to homepage</button></div></div>{loadingRooms ? <p className="notice">Loading rooms...</p> 
CHUNK 2 len=200
: <div className="room-list">{rooms.map((room) => <article className="room-card" key={room.id}><div><h3>{room.name}</h3><p>{room.room_type}</p></div><div><strong>{formatCurrency(room.price_per_night)}
CHUNK 3 len=200
</strong><p>Up to {room.capacity} guests</p><button onClick={() => handleReserve(room)}>Reserve</button></div></article>)}</div>}</section> : page === 'home' && !signedIn ? <section className="hero"><
CHUNK 4 len=200
div className="hero-copy"><p className="eyebrow">DISCOVER BANGLADESH</p><h1>Find your next<br /><em>perfect stay.</em></h1><p className="subcopy">Beautiful places, memorable moments, and stays made si
CHUNK 5 len=200
mple.</p></div></section> : <section className="section booking-hero"><div className="section-top"><div><p className="eyebrow">BOOKING</p><h2>{bookingHeadline}</h2><p>{bookingSubtitle}</p></div></div>
CHUNK 6 len=200
<div className="search"><label>Where <input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="City or hotel" /></label><label>Check in <input type="date" value={checkIn} 
CHUNK 7 len=200
onChange={(event) => setCheckIn(event.target.value)} /></label><label>Check out <input type="date" value={checkOut} onChange={(event) => setCheckOut(event.target.value)} /></label><label>Guests <selec
CHUNK 8 len=200
t value={guests} onChange={(event) => setGuests(event.target.value)}><option>2 guests</option><option>1 guest</option><option>3 guests</option><option>4 guests</option></select></label><button classNa
CHUNK 9 len=200
me="search-btn" onClick={handleSearch}>Search stays →</button></div><div className="cards">{visible.map((hotel) => <article className="card" key={hotel.id}><img src={hotel.image_url || sampleHotels[0]
CHUNK 10 len=200
.image_url} alt={hotel.name} /><div className="card-body"><div className="card-title"><h3>{hotel.name}</h3><span>★ {hotel.rating}</span></div><p className="location">⌖ {hotel.city}</p><p>{hotel.descri
CHUNK 11 len=200
ption}</p><div className="amenities">{(hotel.amenities || []).slice(0, 3).map((item) => <small key={item}>{item}</small>)}</div><button onClick={() => handleViewRooms(hotel)}>Book Rooms</button></div>
CHUNK 12 len=29
</article>)}</div></section>}
